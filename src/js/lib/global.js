
define(['jquery', 'backbone', 'underscore', 'moment', 'template'], ($, Backbone, _, moment, template) => {
    window.$ = $;
    window._ = _;
    window.Backbone = Backbone;
    window.moment = moment;
    window.template = template;
});
